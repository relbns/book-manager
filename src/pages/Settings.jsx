import React from 'react';
import { Typography, Card } from 'antd';

const { Title } = Typography;

const Settings = () => {
  return (
    <Card>
      <Title level={2}>הגדרות</Title>
      <p>עמוד הגדרות - בקרוב</p>
    </Card>
  );
};

export default Settings;
